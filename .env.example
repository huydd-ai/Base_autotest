# Copy to .env and adjust. All optional (code has defaults / reads games/<GAME>/game.json).
GAME=demo
# These override the game profile when set:
# DEVICE_URI=Android://127.0.0.1:5037/127.0.0.1:5555
# APP_PACKAGE=com.example.puzzle
OCR_THRESHOLD=0.7
DEFAULT_TIMEOUT=10
LOG_LEVEL=INFO
