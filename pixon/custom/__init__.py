"""Custom/extensible page implementations.

Review flag: speculative bucket. Kept empty per the reference tree but YAGNI —
add a module here only when a real game-specific override exists. Until then it
holds nothing on purpose.
"""
