[pytest]
testpaths = Test
addopts = -v
markers =
    smoke: quick end-to-end sanity check on the device
    daily_mission: Daily Mission suite
    heart_system: Heart System suite
    player_profile: Player Profile suite (legacy)
